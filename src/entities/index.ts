import { Inventory } from './inventory.entity';
import { Product } from './product.entity';
import { Sale } from './sale.entity';

export {
    Inventory,
    Product,
    Sale
}

export const entities = [Product, Inventory, Sale];
